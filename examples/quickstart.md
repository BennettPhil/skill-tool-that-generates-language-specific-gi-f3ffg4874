# Quickstart

Successful run:

```bash
./scripts/run.sh --mode brief
```

Expected output starts with:

```text
## Idea Summary
```

Failure run:

```bash
./scripts/run.sh --mode invalid
```

Expected stderr:

```text
Invalid mode: invalid
```
